//Write a function called getValue which accepts no parameters.
//It will ask the user to enter positive numbers (use non-positive
// to stop).  It will return the largest of the values
//entered by the user
int getValue(){
	int input;
	int biggest;
}